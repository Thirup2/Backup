优先级和结合律规定了运算对象的组合方式，但是没有说明运算对象按照什么顺序求值。在大多数情况下，不会明确指定求值的顺序。
如：
```c++
int i = f1() + f2();
```
对于这个加法运算，首先需要对其两边的操作数求值，但无法确定先对`f1()`求值还是先对`f2()`求值。
**注意**：在一个表达式中，如果某个对象的求值会对另一个对象的值产生影响，产生的效果是未定义的。无法推断表达式的求值顺序。